# Sanitize one startup track specification

Sanitize one startup track specification

## Usage

``` r
.sanitizeTrack(track, depth)
```

## Arguments

- track:

  A named list, one igv.js track configuration.

- depth:

  An integer, how deep this track sits in a merged track.

## Value

The sanitized track, or NULL if it cannot be used.
