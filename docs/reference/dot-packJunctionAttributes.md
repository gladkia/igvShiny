# Pack junction attributes into a bed name column

Pack junction attributes into a bed name column

## Usage

``` r
.packJunctionAttributes(tbl)
```

## Arguments

- tbl:

  A data.frame of junctions.

## Value

A character vector, one packed name field per row.
