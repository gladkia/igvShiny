# get_cas_genomes

a helper function for common always available stock genomes, obtains the
genome codes (e.g. 'hg38')

## Usage

``` r
get_cas_genomes()
```

## Value

an list of short genome codes, e.g., "hg38", "dm6"

## Examples

``` r
cas <- get_cas_genomes()
```
